<form method="post" action="{{ path('app_admin_fistsplit_delete', {'id': fitsplit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fitsplit.id) }}">
    <button class="btn">Delete</button>
</form>
